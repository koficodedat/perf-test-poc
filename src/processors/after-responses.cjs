/**
 * write all afterResponse functions here.
 * it should be in the form: "function some_function_name(req, res, context, events, done) {}"
 */

module.exports = {
    /**
     * export all afterResponse functions here so you can use it in your script 
     * it should be comma separated in the form:
     *  some_function_name_one,
     *  some_function_name_two,
     *  some_function_name_three
     */
}